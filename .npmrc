shamefully-hoist=false
strict-peer-dependencies=true

# GitHub Packages registry for @premast scope
@premast:registry=https://npm.pkg.github.com
# Auth token is set via GITHUB_TOKEN env var or ~/.npmrc on developer machines.
# In CI, the actions/setup-node step handles this automatically.
